#version 410

// in vec2 fragTexCoord;
in vec4 fragColor;

out vec4 finalColor;

// uniform sampler2D texture0;
// uniform vec4 colDiffuse;
// uniform vec2 positions[30000];
//
void main() {
    finalColor = vec4(1.0);
}
